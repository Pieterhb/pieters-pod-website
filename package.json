{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backup": "git add -A && git commit -m \"Backup latest changes\" && git push"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
